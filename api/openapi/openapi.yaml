openapi: 3.0.3
info:
  title: goimg-datalayer API
  version: 1.0.0
  description: |
    A self-hosted image gallery backend inspired by Flickr and Chevereto.

    Features:
    - User management with JWT authentication
    - Image upload with automatic variant generation
    - Albums, tags, and social features (likes, comments)
    - Content moderation and abuse reporting
    - RESTful API with RFC 7807 error responses

    **Phase 2 Endpoints**: Some endpoints are marked with `x-phase: "2"` extension
    to indicate they are planned for Phase 2 development and not yet implemented.
    These include: moderation endpoints, advanced tag endpoints.

  contact:
    name: goimg-datalayer API Team
    url: https://github.com/yegamble/goimg-datalayer
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT

servers:
  - url: http://localhost:8080/api/v1
    description: Development server
  - url: https://api.goimg.com/v1
    description: Production server (example)

security:
  - bearerAuth: []

tags:
  - name: auth
    description: Authentication and authorization endpoints
  - name: users
    description: User profile management
  - name: images
    description: Image upload, retrieval, and management
  - name: albums
    description: Album creation and management
  - name: tags
    description: Tag operations and image tagging
  - name: social
    description: Social features (likes, comments)
  - name: notifications
    description: In-app notifications and alerts
  - name: moderation
    description: Content moderation and abuse reporting
  - name: explore
    description: Discovery and exploration features
  - name: health
    description: Health check endpoints
  - name: monitoring
    description: Application monitoring and metrics

paths:
  # ==================== Authentication ====================
  /auth/register:
    post:
      tags: [auth]
      summary: Register a new user
      description: |
        Create a new user account with email, username, and password.

        Validation:
        - Email must be unique and not from disposable provider
        - Username must be unique, 3-32 chars, alphanumeric
        - Password must be 12-128 chars with complexity requirements
        - Password is checked against Have I Been Pwned (HIBP) database using k-anonymity
          (only first 5 chars of SHA-1 hash are sent, preserving privacy)

        Security:
        - Compromised passwords (found in data breaches) are rejected
        - HIBP check uses k-anonymity to protect user privacy
        - If HIBP service is unavailable, registration proceeds (fail-open)
      operationId: registerUser
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, username, password]
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 255
                  example: user@example.com
                username:
                  type: string
                  minLength: 3
                  maxLength: 32
                  pattern: '^[a-zA-Z0-9_-]+$'
                  example: johndoe
                password:
                  type: string
                  format: password
                  minLength: 12
                  maxLength: 128
                  example: MySecureP@ssw0rd123
      responses:
        '201':
          description: User created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    format: uuid
                  email:
                    type: string
                  username:
                    type: string
                  created_at:
                    type: string
                    format: date-time
        '400':
          description: |
            Validation error. Possible error types:
            - `validation-error`: Field validation failed (e.g., password too short)
            - `password-compromised`: Password found in a data breach (HIBP check)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              examples:
                validation_error:
                  summary: Field validation error
                  value:
                    type: https://api.goimg.com/problems/validation-error
                    title: Validation Error
                    status: 400
                    detail: Request validation failed
                    traceId: abc123-def456
                    errors:
                      - field: password
                        message: password must be at least 12 characters
                password_compromised:
                  summary: Password found in data breach
                  value:
                    type: https://api.goimg.com/problems/password-compromised
                    title: Password Compromised
                    status: 400
                    detail: This password has been found in a data breach and cannot be used. Please choose a different password.
                    traceId: abc123-def456
        '409':
          description: Email or username already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              example:
                type: https://api.goimg.com/problems/conflict
                title: Conflict
                status: 409
                detail: Email or username already exists
                traceId: abc123-def456

  /auth/login:
    post:
      tags: [auth]
      summary: User login
      description: |
        Authenticate with email and password to receive JWT tokens.

        Security:
        - Rate limit: 5 attempts per minute per IP
        - Account lockout after 5 failed attempts (15 min)
        - Generic error messages to prevent account enumeration
        - Random response delay (100-300ms) to prevent timing attacks
        - Constant-time password comparison (Argon2id)
      operationId: loginUser
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email:
                  type: string
                  format: email
                  example: user@example.com
                password:
                  type: string
                  format: password
                  example: MySecureP@ssw0rd123
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
        '401':
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              example:
                type: https://api.goimg.com/problems/unauthorized
                title: Unauthorized
                status: 401
                detail: Invalid email or password
                traceId: abc123-def456
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /auth/refresh:
    post:
      tags: [auth]
      summary: Refresh access token
      description: |
        Exchange a refresh token for a new access token and refresh token.

        Security:
        - Refresh token rotation (old token invalidated)
        - Replay attack detection (token reuse revokes entire family)
      operationId: refreshToken
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [refresh_token]
              properties:
                refresh_token:
                  type: string
                  example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
      responses:
        '200':
          description: Token refreshed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
        '401':
          description: Invalid or expired refresh token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /auth/logout:
    post:
      tags: [auth]
      summary: User logout
      description: Invalidate the current refresh token
      operationId: logoutUser
      responses:
        '204':
          description: Logout successful
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  # ==================== Two-Factor Authentication ====================
  /auth/2fa/setup:
    post:
      tags: [auth]
      summary: Setup 2FA
      description: |
        Initiate two-factor authentication setup for the authenticated user.

        This endpoint generates:
        - A TOTP secret for use with authenticator apps (Google Authenticator, Authy, etc.)
        - A provisioning URI for QR code generation
        - 10 one-time backup codes for account recovery

        Security:
        - Requires authentication
        - Returns secret and backup codes only once - user must save them
        - 2FA is not active until verified with /auth/2fa/verify
        - Backup codes are hashed before storage (bcrypt)
        - TOTP secret is encrypted at rest (AES-256-GCM)
      operationId: setup2FA
      responses:
        '200':
          description: 2FA setup initiated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Setup2FAResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '409':
          description: 2FA already enabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              example:
                type: https://api.goimg.com/problems/conflict
                title: Conflict
                status: 409
                detail: Two-factor authentication is already enabled
                traceId: abc123-def456

  /auth/2fa/verify:
    post:
      tags: [auth]
      summary: Verify and enable 2FA
      description: |
        Verify the TOTP code and enable two-factor authentication.

        This must be called after /auth/2fa/setup with a valid 6-digit code
        from the user's authenticator app.

        Security:
        - Code validation uses constant-time comparison
        - Allows 1 time-step skew (±30 seconds)
        - Once verified, 2FA is required for all future logins
      operationId: verify2FA
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code:
                  type: string
                  pattern: '^[0-9]{6}$'
                  description: 6-digit TOTP code from authenticator app
                  example: "123456"
      responses:
        '200':
          description: 2FA enabled successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Two-factor authentication has been enabled successfully
        '400':
          description: Invalid verification code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              example:
                type: https://api.goimg.com/problems/bad-request
                title: Bad Request
                status: 400
                detail: Invalid verification code
                traceId: abc123-def456
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: No pending 2FA setup found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '409':
          description: 2FA already enabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /auth/2fa/disable:
    post:
      tags: [auth]
      summary: Disable 2FA
      description: |
        Disable two-factor authentication for the authenticated user.

        Security:
        - Requires password confirmation
        - Optionally accepts TOTP code for additional verification
        - Deletes TOTP secret and all backup codes
        - Publishes audit event for security logging
      operationId: disable2FA
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [password]
              properties:
                password:
                  type: string
                  format: password
                  description: Current account password for confirmation
                  example: MySecureP@ssw0rd123
                code:
                  type: string
                  pattern: '^[0-9]{6}$'
                  description: Optional 6-digit TOTP code for extra security
                  example: "123456"
      responses:
        '200':
          description: 2FA disabled successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Two-factor authentication has been disabled
        '400':
          description: Invalid request or TOTP code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '401':
          description: Unauthorized or invalid password
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: 2FA not enabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /auth/2fa/status:
    get:
      tags: [auth]
      summary: Get 2FA status
      description: |
        Retrieve the current two-factor authentication status for the authenticated user.

        Returns:
        - Whether 2FA is enabled
        - Whether setup is pending verification
        - Number of unused backup codes remaining
        - When 2FA was enabled (if applicable)
      operationId: get2FAStatus
      responses:
        '200':
          description: 2FA status retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TwoFactorStatus'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /auth/2fa/backup-codes/regenerate:
    post:
      tags: [auth]
      summary: Regenerate backup codes
      description: |
        Generate new backup codes, invalidating all existing ones.

        Security:
        - Requires password confirmation
        - 2FA must be enabled
        - Old backup codes are deleted before generating new ones
        - Returns 10 new one-time codes
        - Codes are shown only once - user must save them
      operationId: regenerateBackupCodes
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [password]
              properties:
                password:
                  type: string
                  format: password
                  description: Current account password for confirmation
                  example: MySecureP@ssw0rd123
      responses:
        '200':
          description: Backup codes regenerated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BackupCodesResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '401':
          description: Unauthorized or invalid password
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: 2FA not enabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  # ==================== OAuth 2.0 ====================
  /auth/oauth/{provider}:
    get:
      tags: [auth]
      summary: Initiate OAuth flow
      description: |
        Redirect to OAuth provider authorization page.

        Supported providers:
        - google: Google OAuth 2.0
        - github: GitHub OAuth

        This endpoint redirects the user to the OAuth provider's authorization page.
        After user consent, the provider redirects back to the callback endpoint.

        Security:
        - State parameter is generated and validated to prevent CSRF attacks
        - PKCE (Proof Key for Code Exchange) is used for enhanced security
      operationId: initiateOAuthFlow
      security: []
      parameters:
        - name: provider
          in: path
          required: true
          description: OAuth provider name
          schema:
            type: string
            enum: [google, github]
          example: google
      responses:
        '302':
          description: Redirect to OAuth provider
          headers:
            Location:
              description: OAuth provider authorization URL
              schema:
                type: string
                format: uri
                example: https://accounts.google.com/o/oauth2/v2/auth?client_id=...
        '400':
          description: Invalid provider
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              example:
                type: https://api.goimg.com/problems/bad-request
                title: Bad Request
                status: 400
                detail: Invalid OAuth provider
                traceId: abc123-def456

  /auth/oauth/{provider}/callback:
    get:
      tags: [auth]
      summary: Handle OAuth callback
      description: |
        Handle OAuth provider callback after user authorization.

        This endpoint receives the authorization code from the OAuth provider,
        exchanges it for an access token, and either:
        - Creates a new user account (if email doesn't exist)
        - Logs in existing user (if OAuth account is linked)
        - Returns error if account exists but OAuth is not linked

        Security:
        - Validates state parameter to prevent CSRF
        - Verifies PKCE code verifier
        - Token exchange happens server-side
      operationId: handleOAuthCallback
      security: []
      parameters:
        - name: provider
          in: path
          required: true
          description: OAuth provider name
          schema:
            type: string
            enum: [google, github]
          example: google
        - name: code
          in: query
          required: true
          description: Authorization code from OAuth provider
          schema:
            type: string
          example: 4/0AfJohXm...
        - name: state
          in: query
          required: true
          description: CSRF protection state parameter
          schema:
            type: string
          example: abc123def456
        - name: error
          in: query
          description: Error code from OAuth provider (if authorization failed)
          schema:
            type: string
          example: access_denied
        - name: error_description
          in: query
          description: Error description from OAuth provider
          schema:
            type: string
          example: User denied access
      responses:
        '200':
          description: OAuth login successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
        '400':
          description: |
            OAuth error. Possible error types:
            - `invalid-state`: State parameter mismatch (CSRF attack)
            - `invalid-code`: Authorization code invalid or expired
            - `oauth-error`: Provider returned an error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              examples:
                invalid_state:
                  summary: Invalid state parameter
                  value:
                    type: https://api.goimg.com/problems/invalid-state
                    title: Invalid State
                    status: 400
                    detail: State parameter validation failed
                    traceId: abc123-def456
                oauth_error:
                  summary: OAuth provider error
                  value:
                    type: https://api.goimg.com/problems/oauth-error
                    title: OAuth Error
                    status: 400
                    detail: User denied access
                    traceId: abc123-def456
        '409':
          description: Email already registered with different authentication method
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              example:
                type: https://api.goimg.com/problems/conflict
                title: Conflict
                status: 409
                detail: An account with this email already exists. Please use password login or link your OAuth account.
                traceId: abc123-def456

  /auth/oauth/link:
    post:
      tags: [auth]
      summary: Link OAuth account
      description: |
        Link an OAuth provider account to the authenticated user.

        This allows users to add OAuth login methods to their existing account.
        The OAuth account's email must match the user's account email.

        Process:
        1. User initiates OAuth flow on frontend
        2. After OAuth provider authorization, frontend receives code and state
        3. Frontend sends code and state to this endpoint
        4. Server exchanges code for OAuth account info and links to user

        Security:
        - Requires JWT authentication
        - Validates state parameter
        - Email must match authenticated user's email
      operationId: linkOAuthAccount
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [provider, code, state]
              properties:
                provider:
                  type: string
                  enum: [google, github]
                  example: google
                code:
                  type: string
                  description: Authorization code from OAuth provider
                  example: 4/0AfJohXm...
                state:
                  type: string
                  description: CSRF protection state parameter
                  example: abc123def456
      responses:
        '200':
          description: OAuth account linked successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthAccount'
        '400':
          description: |
            Validation error. Possible error types:
            - `invalid-state`: State parameter mismatch
            - `email-mismatch`: OAuth email doesn't match user's email
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              examples:
                email_mismatch:
                  summary: Email mismatch
                  value:
                    type: https://api.goimg.com/problems/email-mismatch
                    title: Email Mismatch
                    status: 400
                    detail: OAuth account email does not match your account email
                    traceId: abc123-def456
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '409':
          description: OAuth account already linked to another user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              example:
                type: https://api.goimg.com/problems/conflict
                title: Conflict
                status: 409
                detail: This OAuth account is already linked to another user
                traceId: abc123-def456

    delete:
      tags: [auth]
      summary: Unlink OAuth account
      description: |
        Remove OAuth provider link from the authenticated user.

        Security:
        - Users cannot unlink their only authentication method
        - Must have either password or another OAuth provider linked
      operationId: unlinkOAuthAccount
      parameters:
        - name: provider
          in: query
          required: true
          description: OAuth provider to unlink
          schema:
            type: string
            enum: [google, github]
          example: google
      responses:
        '204':
          description: OAuth account unlinked successfully
        '400':
          description: Cannot unlink only authentication method
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              example:
                type: https://api.goimg.com/problems/bad-request
                title: Bad Request
                status: 400
                detail: Cannot unlink your only authentication method. Add a password or another OAuth provider first.
                traceId: abc123-def456
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: OAuth provider not linked
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              example:
                type: https://api.goimg.com/problems/not-found
                title: Not Found
                status: 404
                detail: OAuth provider not linked to your account
                traceId: abc123-def456

  /auth/oauth/accounts:
    get:
      tags: [auth]
      summary: List linked OAuth accounts
      description: |
        Retrieve list of OAuth providers linked to the authenticated user.

        Returns information about connected OAuth accounts including:
        - Provider name (google, github)
        - Provider user ID
        - Email used for OAuth
        - Display name from provider
        - When the account was linked
      operationId: listLinkedOAuthAccounts
      responses:
        '200':
          description: OAuth accounts retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  accounts:
                    type: array
                    items:
                      $ref: '#/components/schemas/OAuthAccount'
                  total_count:
                    type: integer
                    description: Number of linked OAuth accounts
                    example: 2
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  # ==================== Users ====================
  /users/{id}:
    get:
      tags: [users]
      summary: Get user profile
      description: Retrieve public profile information for a user
      operationId: getUser
      security:
        - {}
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/UserIdParam'
      responses:
        '200':
          description: User profile retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

    put:
      tags: [users]
      summary: Update user profile
      description: |
        Update own user profile. Users can only update their own profile.

        Updatable fields:
        - display_name
        - bio
      operationId: updateUser
      parameters:
        - $ref: '#/components/parameters/UserIdParam'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                display_name:
                  type: string
                  maxLength: 100
                  example: John Doe
                bio:
                  type: string
                  maxLength: 500
                  example: Photographer and nature enthusiast
      responses:
        '200':
          description: Profile updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Forbidden - cannot update another user's profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

    delete:
      tags: [users]
      summary: Delete user account
      description: |
        Delete own user account permanently. This action cannot be undone.
        All user images and albums will be deleted.
      operationId: deleteUser
      parameters:
        - $ref: '#/components/parameters/UserIdParam'
      responses:
        '204':
          description: User account deleted successfully
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Forbidden - cannot delete another user's account
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /users/{id}/sessions:
    get:
      tags: [users]
      summary: List user sessions
      description: |
        Retrieve active sessions for the authenticated user.
        Users can only view their own sessions.
      operationId: listUserSessions
      parameters:
        - $ref: '#/components/parameters/UserIdParam'
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Sessions retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  sessions:
                    type: array
                    items:
                      $ref: '#/components/schemas/Session'
                  pagination:
                    $ref: '#/components/schemas/Pagination'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Forbidden - cannot view another user's sessions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /users/{id}/likes:
    get:
      tags: [users]
      summary: Get user's liked images
      description: |
        Retrieve a paginated list of images that the specified user has liked.

        Access control:
        - Public users: Can view likes of users with public profiles
        - Authenticated users: Can view their own likes and public likes
      operationId: getUserLikedImages
      security:
        - {}
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/UserIdParam'
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Liked images retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: '#/components/schemas/Image'
        '401':
          description: Unauthorized (optional - only if authentication required)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  # ==================== User Follow/Unfollow ====================
  /users/{id}/follow:
    post:
      tags: [users]
      summary: Follow user
      description: |
        Follow another user to receive updates about their activity.

        Business rules:
        - Cannot follow yourself
        - Cannot follow the same user twice
        - Cannot follow inactive users (suspended/deleted)

        This operation is idempotent - following a user you already follow returns 409 Conflict.
      operationId: followUser
      parameters:
        - $ref: '#/components/parameters/UserIdParam'
      responses:
        '204':
          description: User followed successfully
        '400':
          description: |
            Validation error. Possible error types:
            - Invalid user ID format
            - Cannot follow yourself
            - Cannot follow inactive user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              examples:
                cannot_follow_self:
                  summary: Cannot follow yourself
                  value:
                    type: https://api.goimg.com/problems/bad-request
                    title: Bad Request
                    status: 400
                    detail: Cannot follow yourself
                    traceId: abc123-def456
                inactive_user:
                  summary: Cannot follow inactive user
                  value:
                    type: https://api.goimg.com/problems/bad-request
                    title: Bad Request
                    status: 400
                    detail: Cannot follow inactive user
                    traceId: abc123-def456
        '401':
          description: Unauthorized - authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: Target user not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              example:
                type: https://api.goimg.com/problems/not-found
                title: Not Found
                status: 404
                detail: User not found
                traceId: abc123-def456
        '409':
          description: Already following this user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
              example:
                type: https://api.goimg.com/problems/conflict
                title: Conflict
                status: 409
                detail: You are already following this user
                traceId: abc123-def456

    delete:
      tags: [users]
      summary: Unfollow user
      description: |
        Unfollow a user to stop receiving updates about their activity.

        This operation is idempotent - unfollowing a user you don't follow succeeds without error.
        This prevents client-side retry issues and simplifies error handling.
      operationId: unfollowUser
      parameters:
        - $ref: '#/components/parameters/UserIdParam'
      responses:
        '204':
          description: User unfollowed successfully (or was not following)
        '400':
          description: Invalid user ID format
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '401':
          description: Unauthorized - authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /users/{id}/followers:
    get:
      tags: [users]
      summary: List user followers
      description: |
        Retrieve a paginated list of users who are following the specified user.

        Returns user profile information for each follower including:
        - User ID, username, display name
        - Bio (if set)
        - When they started following (followed_at timestamp)

        This endpoint is public - anyone can view a user's followers.
      operationId: getUserFollowers
      security:
        - {}
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/UserIdParam'
        - name: limit
          in: query
          description: Maximum number of followers to return
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
          example: 20
        - name: offset
          in: query
          description: Number of followers to skip for pagination
          schema:
            type: integer
            minimum: 0
            default: 0
          example: 0
      responses:
        '200':
          description: Followers list retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FollowersList'
        '400':
          description: Invalid user ID or pagination parameters
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /users/{id}/following:
    get:
      tags: [users]
      summary: List users being followed
      description: |
        Retrieve a paginated list of users that the specified user is following.

        Returns user profile information for each followed user including:
        - User ID, username, display name
        - Bio (if set)
        - When follow started (followed_at timestamp)

        This endpoint is public - anyone can view who a user is following.
      operationId: getUserFollowing
      security:
        - {}
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/UserIdParam'
        - name: limit
          in: query
          description: Maximum number of followed users to return
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
          example: 20
        - name: offset
          in: query
          description: Number of followed users to skip for pagination
          schema:
            type: integer
            minimum: 0
            default: 0
          example: 0
      responses:
        '200':
          description: Following list retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FollowingList'
        '400':
          description: Invalid user ID or pagination parameters
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /feed:
    get:
      tags: [social]
      summary: Get activity feed
      description: |
        Retrieve a paginated feed of activities from users that the authenticated user follows.

        Returns activities including:
        - Image uploads, likes, and comments
        - User follows
        - Album creations

        Activities are sorted by creation time (most recent first) and include metadata
        for displaying activity details in the UI.

        This endpoint requires authentication - only the authenticated user can view their own feed.
      operationId: getActivityFeed
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of activities to return
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
          example: 20
        - name: offset
          in: query
          description: Number of activities to skip for pagination
          schema:
            type: integer
            minimum: 0
            default: 0
          example: 0
      responses:
        '200':
          description: Activity feed retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityFeed'
        '400':
          description: Invalid pagination parameters
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  # ==================== Notifications ====================
  /notifications:
    get:
      tags: [notifications]
      summary: Get notifications
      description: |
        Retrieve a paginated list of notifications for the authenticated user.

        Notifications include:
        - New followers
        - New photos from followed users
        - Account status changes (suspended, banned, reinstated)
        - Admin/moderator alerts (for admin users)

        Notifications are sorted by creation time (most recent first) and include
        a read/unread status for badge display in UI.

        This endpoint requires authentication - users can only view their own notifications.
      operationId: getNotifications
      security:
        - bearerAuth: []
      parameters:
        - name: unread_only
          in: query
          description: If true, only return unread notifications
          schema:
            type: boolean
            default: false
          example: false
        - name: limit
          in: query
          description: Maximum number of notifications to return
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
          example: 20
        - name: offset
          in: query
          description: Number of notifications to skip for pagination
          schema:
            type: integer
            minimum: 0
            default: 0
          example: 0
      responses:
        '200':
          description: Notifications retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationsList'
        '400':
          description: Invalid query parameters
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /notifications/count:
    get:
      tags: [notifications]
      summary: Get unread notification count
      description: |
        Retrieve the count of unread notifications for the authenticated user.

        This endpoint is lightweight and designed for polling to update
        notification badges in the UI. Returns only the count, not the
        notification details.

        This endpoint requires authentication - users can only view their own count.
      operationId: getUnreadNotificationCount
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Unread count retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnreadCount'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /notifications/read:
    post:
      tags: [notifications]
      summary: Mark notifications as read
      description: |
        Mark one or more notifications as read for the authenticated user.

        This endpoint supports two modes:
        1. Mark specific notifications by ID (provide notificationIds array)
        2. Mark all notifications as read (set markAllAsRead to true)

        Marking already-read notifications is idempotent and will succeed.

        Authorization: Users can only mark their own notifications as read.
        Attempting to mark another user's notifications returns 403 Forbidden.
      operationId: markNotificationsAsRead
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                notificationIds:
                  type: array
                  description: Array of notification IDs to mark as read (optional)
                  items:
                    type: string
                    format: uuid
                  example: ["123e4567-e89b-12d3-a456-426614174001", "123e4567-e89b-12d3-a456-426614174002"]
                markAllAsRead:
                  type: boolean
                  description: If true, mark all notifications for user as read (optional)
                  default: false
                  example: false
              anyOf:
                - required: [notificationIds]
                - required: [markAllAsRead]
      responses:
        '204':
          description: Notifications marked as read successfully
        '400':
          description: Invalid request body (neither notificationIds nor markAllAsRead provided)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Forbidden - notification does not belong to user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: Notification not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  # ==================== Images ====================
  /images:
    post:
      tags: [images]
      summary: Upload image
      description: |
        Upload a new image with optional metadata.

        Processing:
        - ClamAV malware scan (required)
        - EXIF metadata extraction
        - EXIF stripping (GPS data removed by default)
        - Generate 4 variants: thumbnail (150px), small (320px), medium (800px), large (1600px)
        - Store original
        - Re-encode through libvips for security

        Rate limit: 50 uploads per hour per user
      operationId: uploadImage
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file (JPEG, PNG, GIF, WebP)
                title:
                  type: string
                  maxLength: 255
                  example: Sunset over mountains
                description:
                  type: string
                  maxLength: 2000
                  example: Beautiful sunset captured in Rocky Mountains
                visibility:
                  type: string
                  enum: [public, private, unlisted]
                  default: private
                album_id:
                  type: string
                  format: uuid
                  description: Add image to album (optional)
                tags:
                  type: string
                  description: Comma-separated tags (max 20)
                  example: sunset,mountains,nature,landscape
      responses:
        '201':
          description: Image uploaded successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Image'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '413':
          description: File too large (max 10MB)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '415':
          description: Unsupported media type
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '422':
          description: Malware detected or processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '429':
          description: Upload rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

    get:
      tags: [images]
      summary: List images
      description: |
        List images with optional filtering and pagination.

        Returns only images the authenticated user has permission to view:
        - Own images (all visibility levels)
        - Public images from other users
      operationId: listImages
      security:
        - {}
        - bearerAuth: []
      parameters:
        - name: owner_id
          in: query
          description: Filter by image owner
          schema:
            type: string
            format: uuid
        - name: album_id
          in: query
          description: Filter by album
          schema:
            type: string
            format: uuid
        - name: visibility
          in: query
          description: Filter by visibility (only for own images)
          schema:
            type: string
            enum: [public, private, unlisted]
        - name: tags
          in: query
          description: Filter by tags (comma-separated, AND logic)
          schema:
            type: string
            example: sunset,nature
        - name: q
          in: query
          description: Search query (title, description)
          schema:
            type: string
        - name: sort
          in: query
          description: Sort field
          schema:
            type: string
            enum: [created_at, view_count, like_count]
            default: created_at
        - name: order
          in: query
          description: Sort order
          schema:
            type: string
            enum: [asc, desc]
            default: desc
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Images retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: '#/components/schemas/Image'
        '400':
          description: Invalid query parameters
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /images/{id}:
    get:
      tags: [images]
      summary: Get image details
      description: Retrieve detailed information about a specific image
      operationId: getImage
      security:
        - {}
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ImageIdParam'
      responses:
        '200':
          description: Image retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Image'
        '404':
          description: Image not found or not accessible
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

    put:
      tags: [images]
      summary: Update image metadata
      description: |
        Update image metadata. Only the image owner can update their images.
        Admins can update any image.
      operationId: updateImage
      parameters:
        - $ref: '#/components/parameters/ImageIdParam'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                  maxLength: 255
                description:
                  type: string
                  maxLength: 2000
                visibility:
                  type: string
                  enum: [public, private, unlisted]
                tags:
                  type: array
                  items:
                    type: string
                  maxItems: 20
                  example: [sunset, mountains, nature]
      responses:
        '200':
          description: Image updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Image'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Forbidden - cannot update another user's image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: Image not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

    delete:
      tags: [images]
      summary: Delete image
      description: |
        Delete an image permanently. Only the image owner can delete their images.
        Moderators and admins can delete any image.
      operationId: deleteImage
      parameters:
        - $ref: '#/components/parameters/ImageIdParam'
      responses:
        '204':
          description: Image deleted successfully
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Forbidden - cannot delete another user's image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: Image not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /images/{id}/variants/{size}:
    get:
      tags: [images]
      summary: Get image variant
      description: |
        Retrieve a specific image variant (resized version).

        Available sizes:
        - thumbnail: 160px max width (WebP, quality 82)
        - small: 320px max width (WebP, quality 85)
        - medium: 800px max width (WebP, quality 85)
        - large: 1600px max width (WebP, quality 88)
        - original: unchanged dimensions (original format, quality 100 - maximum quality with security re-encoding)

        Aspect ratio is preserved (contain, not crop).
      operationId: getImageVariant
      security:
        - {}
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ImageIdParam'
        - name: size
          in: path
          required: true
          description: Variant size
          schema:
            type: string
            enum: [thumbnail, small, medium, large, original]
      responses:
        '200':
          description: Image variant retrieved successfully
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
            image/png:
              schema:
                type: string
                format: binary
            image/gif:
              schema:
                type: string
                format: binary
            image/webp:
              schema:
                type: string
                format: binary
        '404':
          description: Image or variant not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /images/search:
    get:
      tags: [images]
      summary: Search images
      description: |
        Full-text search across image titles and descriptions.
        Supports filtering by tags, owner, and visibility.
      operationId: searchImages
      security:
        - {}
        - bearerAuth: []
      parameters:
        - name: q
          in: query
          required: true
          description: Search query (searches title and description)
          schema:
            type: string
            minLength: 1
            example: sunset mountains
        - name: tags
          in: query
          description: Filter by tag name
          schema:
            type: string
            example: nature
        - name: owner_id
          in: query
          description: Filter by owner ID
          schema:
            type: string
            format: uuid
        - name: visibility
          in: query
          description: Filter by visibility (only for own images)
          schema:
            type: string
            enum: [public, private, unlisted]
        - name: sort_by
          in: query
          description: Sort field
          schema:
            type: string
            enum: [relevance, created_at, view_count, like_count]
            default: relevance
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Search results retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: '#/components/schemas/Image'
        '400':
          description: Invalid search query
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  # ==================== Albums ====================
  /albums:
    post:
      tags: [albums]
      summary: Create album
      description: Create a new album to organize images
      operationId: createAlbum
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties:
                title:
                  type: string
                  maxLength: 255
                  example: Summer Vacation 2024
                description:
                  type: string
                  maxLength: 2000
                  example: Photos from our trip to the mountains
                visibility:
                  type: string
                  enum: [public, private, unlisted]
                  default: private
                cover_image_id:
                  type: string
                  format: uuid
                  description: Album cover image (defaults to first image)
      responses:
        '201':
          description: Album created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Album'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

    get:
      tags: [albums]
      summary: List user's albums
      description: List albums for the authenticated user or a specific user
      operationId: listAlbums
      security:
        - {}
        - bearerAuth: []
      parameters:
        - name: owner_id
          in: query
          description: Filter by album owner (defaults to authenticated user)
          schema:
            type: string
            format: uuid
        - name: visibility
          in: query
          description: Filter by visibility (only for own albums)
          schema:
            type: string
            enum: [public, private, unlisted]
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Albums retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: '#/components/schemas/Album'

  /albums/{id}:
    get:
      tags: [albums]
      summary: Get album details
      description: |
        Retrieve album metadata (title, description, visibility, counts).
        Use GET /albums/{id}/images to retrieve images in the album.
      operationId: getAlbum
      security:
        - {}
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AlbumIdParam'
      responses:
        '200':
          description: Album retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Album'
        '404':
          description: Album not found or not accessible
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

    put:
      tags: [albums]
      summary: Update album
      description: Update album metadata. Only the album owner can update their albums.
      operationId: updateAlbum
      parameters:
        - $ref: '#/components/parameters/AlbumIdParam'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                  maxLength: 255
                description:
                  type: string
                  maxLength: 2000
                visibility:
                  type: string
                  enum: [public, private, unlisted]
                cover_image_id:
                  type: string
                  format: uuid
      responses:
        '200':
          description: Album updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Album'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Forbidden - cannot update another user's album
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: Album not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

    delete:
      tags: [albums]
      summary: Delete album
      description: |
        Delete an album permanently. Images in the album are not deleted.
        Only the album owner can delete their albums.
      operationId: deleteAlbum
      parameters:
        - $ref: '#/components/parameters/AlbumIdParam'
      responses:
        '204':
          description: Album deleted successfully
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Forbidden - cannot delete another user's album
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: Album not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /albums/{id}/images:
    get:
      tags: [albums]
      summary: List images in album
      description: |
        Retrieve a paginated list of images belonging to an album.
        Public albums are accessible to everyone; private/unlisted albums
        require the owner to be authenticated.
      operationId: getAlbumImages
      security:
        - {}
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AlbumIdParam'
        - name: offset
          in: query
          description: Number of items to skip
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: limit
          in: query
          description: Maximum number of items to return
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        '200':
          description: Images retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  images:
                    type: array
                    items:
                      $ref: '#/components/schemas/Image'
                  total_count:
                    type: integer
                    description: Total number of images in the album
                  offset:
                    type: integer
                  limit:
                    type: integer
                  has_more:
                    type: boolean
        '404':
          description: Album not found or not accessible
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

    post:
      tags: [albums]
      summary: Add images to album
      description: |
        Add one or more images to an album. Images can belong to multiple albums.
        Only the album owner can add images.
      operationId: addImagesToAlbum
      parameters:
        - $ref: '#/components/parameters/AlbumIdParam'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [image_ids]
              properties:
                image_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
                  maxItems: 100
                  example: [123e4567-e89b-12d3-a456-426614174000, 123e4567-e89b-12d3-a456-426614174001]
      responses:
        '200':
          description: Images added to album successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  added_count:
                    type: integer
                    example: 2
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Forbidden - cannot modify another user's album
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: Album not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /albums/{id}/images/{imageId}:
    delete:
      tags: [albums]
      summary: Remove image from album
      description: |
        Remove an image from an album. The image itself is not deleted.
        Only the album owner can remove images.
      operationId: removeImageFromAlbum
      parameters:
        - $ref: '#/components/parameters/AlbumIdParam'
        - name: imageId
          in: path
          required: true
          description: Image ID
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Image removed from album successfully
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Forbidden - cannot modify another user's album
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: Album or image not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  # ==================== Tags (Phase 2) ====================
  /tags:
    get:
      x-phase: "2"
      tags: [tags]
      summary: List popular tags
      description: Retrieve a list of popular tags with usage counts
      operationId: listPopularTags
      security:
        - {}
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          description: Number of tags to return
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        '200':
          description: Tags retrieved successfully
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Tag'

  /tags/search:
    get:
      x-phase: "2"
      tags: [tags]
      summary: Search tags
      description: Search for tags by prefix (for autocomplete)
      operationId: searchTags
      security:
        - {}
        - bearerAuth: []
      parameters:
        - name: q
          in: query
          required: true
          description: Search query (tag prefix)
          schema:
            type: string
            minLength: 2
            example: sun
        - name: limit
          in: query
          description: Number of results to return
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 10
      responses:
        '200':
          description: Tags retrieved successfully
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Tag'

  /tags/{tag}/images:
    get:
      x-phase: "2"
      tags: [tags]
      summary: List images by tag
      description: Retrieve all public images with a specific tag
      operationId: listImagesByTag
      security:
        - {}
        - bearerAuth: []
      parameters:
        - name: tag
          in: path
          required: true
          description: Tag name
          schema:
            type: string
            example: sunset
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Images retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: '#/components/schemas/Image'

  # ==================== Social (Likes) ====================
  /images/{id}/like:
    post:
      tags: [social]
      summary: Like image
      description: Like an image. This operation is idempotent.
      operationId: likeImage
      parameters:
        - $ref: '#/components/parameters/ImageIdParam'
      responses:
        '200':
          description: Image liked successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  liked:
                    type: boolean
                    example: true
                  like_count:
                    type: integer
                    example: 42
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: Image not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

    delete:
      tags: [social]
      summary: Unlike image
      description: Remove like from an image
      operationId: unlikeImage
      parameters:
        - $ref: '#/components/parameters/ImageIdParam'
      responses:
        '200':
          description: Image unliked successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  liked:
                    type: boolean
                    example: false
                  like_count:
                    type: integer
                    example: 41
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: Image not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  # NOTE: GET /images/{id}/likes endpoint planned for future release
  # Will list users who liked an image with pagination
  # Requires: LikeRepository.GetUsersWhoLiked() method implementation

  # ==================== Social (Comments) ====================
  /images/{id}/comments:
    post:
      tags: [social]
      summary: Add comment
      description: Add a comment to an image
      operationId: addComment
      parameters:
        - $ref: '#/components/parameters/ImageIdParam'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [content]
              properties:
                content:
                  type: string
                  minLength: 1
                  maxLength: 1000
                  example: Beautiful photo! Love the colors.
      responses:
        '201':
          description: Comment added successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Comment'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: Image not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

    get:
      tags: [social]
      summary: List comments
      description: Retrieve all comments for an image
      operationId: listComments
      security:
        - {}
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ImageIdParam'
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Comments retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: '#/components/schemas/Comment'

  /comments/{id}:
    delete:
      tags: [social]
      summary: Delete comment
      description: |
        Delete a comment. Can be deleted by:
        - Comment author
        - Image owner
        - Moderator/Admin
      operationId: deleteComment
      parameters:
        - name: id
          in: path
          required: true
          description: Comment ID
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Comment deleted successfully
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Forbidden - cannot delete this comment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: Comment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  # ==================== Moderation (Phase 2) ====================
  /reports:
    post:
      x-phase: "2"
      tags: [moderation]
      summary: Create abuse report
      description: |
        Report an image for abuse, spam, copyright violation, etc.

        Rate limit: 10 reports per hour per user
      operationId: createReport
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [image_id, reason]
              properties:
                image_id:
                  type: string
                  format: uuid
                reason:
                  type: string
                  enum: [spam, inappropriate, copyright, harassment, other]
                  example: inappropriate
                description:
                  type: string
                  maxLength: 1000
                  example: This image contains inappropriate content
      responses:
        '201':
          description: Report created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Report'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Cannot report own images
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /moderation/reports:
    get:
      x-phase: "2"
      tags: [moderation]
      summary: List reports
      description: List all reports (admin/moderator only)
      operationId: listReports
      parameters:
        - name: status
          in: query
          description: Filter by report status
          schema:
            type: string
            enum: [pending, reviewing, resolved, dismissed]
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Reports retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: '#/components/schemas/Report'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Forbidden - requires admin or moderator role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /moderation/reports/{id}:
    get:
      x-phase: "2"
      tags: [moderation]
      summary: Get report details
      description: Retrieve detailed information about a specific report (admin/moderator only)
      operationId: getReport
      parameters:
        - name: id
          in: path
          required: true
          description: Report ID
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Report retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Report'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Forbidden - requires admin or moderator role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: Report not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /moderation/reports/{id}/resolve:
    post:
      x-phase: "2"
      tags: [moderation]
      summary: Resolve report
      description: Resolve a report with an action (admin/moderator only)
      operationId: resolveReport
      parameters:
        - name: id
          in: path
          required: true
          description: Report ID
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [action]
              properties:
                action:
                  type: string
                  enum: [dismiss, warn, remove, ban]
                  example: remove
                resolution:
                  type: string
                  maxLength: 1000
                  example: Image removed for violating content policy
      responses:
        '200':
          description: Report resolved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Report'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Forbidden - requires admin or moderator role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: Report not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  /users/{id}/ban:
    post:
      x-phase: "2"
      tags: [moderation]
      summary: Ban user
      description: Ban a user from the platform (admin only)
      operationId: banUser
      parameters:
        - $ref: '#/components/parameters/UserIdParam'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason:
                  type: string
                  maxLength: 1000
                  example: Multiple content policy violations
                expires_at:
                  type: string
                  format: date-time
                  description: Ban expiration (null for permanent)
                  example: 2024-12-31T23:59:59Z
      responses:
        '200':
          description: User banned successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  user_id:
                    type: string
                    format: uuid
                  banned:
                    type: boolean
                    example: true
                  reason:
                    type: string
                  expires_at:
                    type: string
                    format: date-time
                    nullable: true
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Forbidden - requires admin role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

    delete:
      x-phase: "2"
      tags: [moderation]
      summary: Unban user
      description: Remove ban from a user (admin only)
      operationId: unbanUser
      parameters:
        - $ref: '#/components/parameters/UserIdParam'
      responses:
        '200':
          description: User unbanned successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  user_id:
                    type: string
                    format: uuid
                  banned:
                    type: boolean
                    example: false
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '403':
          description: Forbidden - requires admin role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProblemDetail'

  # ==================== Explore ====================
  /explore/recent:
    get:
      tags: [explore]
      summary: Recent public images
      description: Retrieve recently uploaded public images
      operationId: exploreRecent
      security:
        - {}
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Recent images retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: '#/components/schemas/Image'

  /explore/popular:
    get:
      tags: [explore]
      summary: Popular images
      description: Retrieve popular images sorted by views or likes
      operationId: explorePopular
      security:
        - {}
        - bearerAuth: []
      parameters:
        - name: period
          in: query
          description: Time period for popularity calculation
          schema:
            type: string
            enum: [day, week, month, all]
            default: week
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Popular images retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: '#/components/schemas/Image'

  # ==================== Health ====================
  /health:
    get:
      tags: [health]
      summary: Liveness check
      description: Check if the service is running
      operationId: healthCheck
      security: []
      responses:
        '200':
          description: Service is running
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthStatus'

  /health/ready:
    get:
      tags: [health]
      summary: Readiness check
      description: |
        Check if the service is ready to accept traffic.
        Validates database and Redis connectivity with timeout handling.
      operationId: healthReadiness
      security: []
      responses:
        '200':
          description: All dependencies healthy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthReadyResponse'
        '503':
          description: One or more dependencies unhealthy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthReadyResponse'

  # ==================== Monitoring ====================
  /metrics:
    get:
      tags: [monitoring]
      summary: Prometheus metrics
      description: |
        Exposes application metrics in Prometheus format.

        Metrics include:
        - HTTP request counts and latencies
        - Database connection pool stats
        - Redis connection stats
        - Image processing metrics
      operationId: metrics
      security: []
      responses:
        '200':
          description: Prometheus metrics in text format
          content:
            text/plain:
              schema:
                type: string
                example: |
                  # HELP goimg_http_requests_total Total HTTP requests
                  # TYPE goimg_http_requests_total counter
                  goimg_http_requests_total{method="GET",path="/health",status="200"} 42

# ==================== Components ====================
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        JWT Bearer authentication. Include the access token in the Authorization header:

        ```
        Authorization: Bearer <access_token>
        ```

        Access tokens expire after 15 minutes. Use the refresh endpoint to obtain a new token.

  parameters:
    UserIdParam:
      name: id
      in: path
      required: true
      description: User ID
      schema:
        type: string
        format: uuid
      example: 123e4567-e89b-12d3-a456-426614174000

    ImageIdParam:
      name: id
      in: path
      required: true
      description: Image ID
      schema:
        type: string
        format: uuid
      example: 123e4567-e89b-12d3-a456-426614174000

    AlbumIdParam:
      name: id
      in: path
      required: true
      description: Album ID
      schema:
        type: string
        format: uuid
      example: 123e4567-e89b-12d3-a456-426614174000

    PageParam:
      name: page
      in: query
      description: Page number
      schema:
        type: integer
        minimum: 1
        default: 1
      example: 1

    PerPageParam:
      name: per_page
      in: query
      description: Number of items per page
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
      example: 20

  schemas:
    # ==================== Core Entities ====================
    User:
      type: object
      required: [id, email, username, created_at]
      properties:
        id:
          type: string
          format: uuid
          example: 123e4567-e89b-12d3-a456-426614174000
        email:
          type: string
          format: email
          example: user@example.com
        username:
          type: string
          example: johndoe
        display_name:
          type: string
          nullable: true
          example: John Doe
        bio:
          type: string
          nullable: true
          example: Photographer and nature enthusiast
        avatar_url:
          type: string
          format: uri
          nullable: true
          example: https://cdn.goimg.com/avatars/123e4567.jpg
        image_count:
          type: integer
          description: Number of public images
          example: 42
        created_at:
          type: string
          format: date-time
          example: 2024-01-15T10:30:00Z

    Image:
      type: object
      required: [id, owner_id, owner, visibility, mime_type, width, height, variants, created_at]
      properties:
        id:
          type: string
          format: uuid
          example: 123e4567-e89b-12d3-a456-426614174000
        owner_id:
          type: string
          format: uuid
          example: 123e4567-e89b-12d3-a456-426614174001
        owner:
          type: object
          required: [id, username]
          properties:
            id:
              type: string
              format: uuid
            username:
              type: string
            display_name:
              type: string
              nullable: true
          example:
            id: 123e4567-e89b-12d3-a456-426614174001
            username: johndoe
            display_name: John Doe
        title:
          type: string
          nullable: true
          example: Sunset over mountains
        description:
          type: string
          nullable: true
          example: Beautiful sunset captured in Rocky Mountains
        visibility:
          type: string
          enum: [public, private, unlisted]
          example: public
        mime_type:
          type: string
          example: image/jpeg
        file_size:
          type: integer
          description: File size in bytes
          example: 2048576
        width:
          type: integer
          description: Image width in pixels
          example: 4000
        height:
          type: integer
          description: Image height in pixels
          example: 3000
        variants:
          type: object
          properties:
            thumbnail:
              $ref: '#/components/schemas/ImageVariant'
            small:
              $ref: '#/components/schemas/ImageVariant'
            medium:
              $ref: '#/components/schemas/ImageVariant'
            large:
              $ref: '#/components/schemas/ImageVariant'
            original:
              $ref: '#/components/schemas/ImageVariant'
        tags:
          type: array
          items:
            type: string
          example: [sunset, mountains, nature, landscape]
        view_count:
          type: integer
          example: 150
        like_count:
          type: integer
          example: 42
        comment_count:
          type: integer
          example: 8
        created_at:
          type: string
          format: date-time
          example: 2024-01-15T10:30:00Z
        updated_at:
          type: string
          format: date-time
          example: 2024-01-15T11:00:00Z

    ImageVariant:
      type: object
      required: [url, width, height]
      properties:
        url:
          type: string
          format: uri
          example: https://cdn.goimg.com/images/123e4567/medium.jpg
        width:
          type: integer
          example: 800
        height:
          type: integer
          example: 600

    Album:
      type: object
      required: [id, owner_id, title, visibility, image_count, created_at]
      properties:
        id:
          type: string
          format: uuid
          example: 123e4567-e89b-12d3-a456-426614174000
        owner_id:
          type: string
          format: uuid
          example: 123e4567-e89b-12d3-a456-426614174001
        title:
          type: string
          example: Summer Vacation 2024
        description:
          type: string
          nullable: true
          example: Photos from our trip to the mountains
        visibility:
          type: string
          enum: [public, private, unlisted]
          example: public
        cover_image_id:
          type: string
          format: uuid
          nullable: true
          example: 123e4567-e89b-12d3-a456-426614174002
        cover_image:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Image'
        image_count:
          type: integer
          example: 15
        created_at:
          type: string
          format: date-time
          example: 2024-01-15T10:30:00Z
        updated_at:
          type: string
          format: date-time
          example: 2024-01-15T11:00:00Z

    AlbumWithImages:
      type: object
      required: [id, owner_id, title, visibility, image_count, created_at]
      properties:
        id:
          type: string
          format: uuid
          example: 123e4567-e89b-12d3-a456-426614174000
        owner_id:
          type: string
          format: uuid
          example: 123e4567-e89b-12d3-a456-426614174001
        title:
          type: string
          example: Summer Vacation 2024
        description:
          type: string
          nullable: true
          example: Photos from our trip to the mountains
        visibility:
          type: string
          enum: [public, private, unlisted]
          example: public
        cover_image_id:
          type: string
          format: uuid
          nullable: true
          example: 123e4567-e89b-12d3-a456-426614174002
        cover_image:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Image'
        image_count:
          type: integer
          example: 15
        created_at:
          type: string
          format: date-time
          example: 2024-01-15T10:30:00Z
        updated_at:
          type: string
          format: date-time
          example: 2024-01-15T11:00:00Z
        images:
          type: array
          items:
            $ref: '#/components/schemas/Image'
        pagination:
          $ref: '#/components/schemas/Pagination'

    Tag:
      type: object
      required: [name, slug, usage_count]
      properties:
        name:
          type: string
          example: sunset
        slug:
          type: string
          example: sunset
        usage_count:
          type: integer
          description: Number of images with this tag
          example: 1234

    Comment:
      type: object
      required: [id, user_id, user, image_id, content, created_at]
      properties:
        id:
          type: string
          format: uuid
          example: 123e4567-e89b-12d3-a456-426614174000
        user_id:
          type: string
          format: uuid
          example: 123e4567-e89b-12d3-a456-426614174001
        user:
          type: object
          required: [id, username]
          properties:
            id:
              type: string
              format: uuid
            username:
              type: string
            display_name:
              type: string
              nullable: true
          example:
            id: 123e4567-e89b-12d3-a456-426614174001
            username: johndoe
            display_name: John Doe
        image_id:
          type: string
          format: uuid
          example: 123e4567-e89b-12d3-a456-426614174002
        content:
          type: string
          example: Beautiful photo! Love the colors.
        created_at:
          type: string
          format: date-time
          example: 2024-01-15T10:30:00Z

    Like:
      type: object
      required: [user_id, user, image_id, created_at]
      properties:
        user_id:
          type: string
          format: uuid
          example: 123e4567-e89b-12d3-a456-426614174001
        user:
          type: object
          required: [id, username]
          properties:
            id:
              type: string
              format: uuid
            username:
              type: string
            display_name:
              type: string
              nullable: true
          example:
            id: 123e4567-e89b-12d3-a456-426614174001
            username: johndoe
            display_name: John Doe
        image_id:
          type: string
          format: uuid
          example: 123e4567-e89b-12d3-a456-426614174002
        created_at:
          type: string
          format: date-time
          example: 2024-01-15T10:30:00Z

    Report:
      type: object
      required: [id, reporter_id, reporter, image_id, image, reason, status, created_at]
      properties:
        id:
          type: string
          format: uuid
          example: 123e4567-e89b-12d3-a456-426614174000
        reporter_id:
          type: string
          format: uuid
          example: 123e4567-e89b-12d3-a456-426614174001
        reporter:
          type: object
          required: [id, username]
          properties:
            id:
              type: string
              format: uuid
            username:
              type: string
          example:
            id: 123e4567-e89b-12d3-a456-426614174001
            username: johndoe
        image_id:
          type: string
          format: uuid
          example: 123e4567-e89b-12d3-a456-426614174002
        image:
          type: object
          required: [id, owner_id, title]
          properties:
            id:
              type: string
              format: uuid
            owner_id:
              type: string
              format: uuid
            title:
              type: string
              nullable: true
            thumbnail_url:
              type: string
              format: uri
          example:
            id: 123e4567-e89b-12d3-a456-426614174002
            owner_id: 123e4567-e89b-12d3-a456-426614174003
            title: Inappropriate image
            thumbnail_url: https://cdn.goimg.com/images/123e4567/thumbnail.jpg
        reason:
          type: string
          enum: [spam, inappropriate, copyright, harassment, other]
          example: inappropriate
        description:
          type: string
          nullable: true
          example: This image contains inappropriate content
        status:
          type: string
          enum: [pending, reviewing, resolved, dismissed]
          example: pending
        resolved_by:
          type: object
          nullable: true
          properties:
            id:
              type: string
              format: uuid
            username:
              type: string
        resolved_at:
          type: string
          format: date-time
          nullable: true
          example: 2024-01-15T12:00:00Z
        resolution:
          type: string
          nullable: true
          example: Image removed for violating content policy
        created_at:
          type: string
          format: date-time
          example: 2024-01-15T10:30:00Z

    # ==================== User Follow/Following ====================
    FollowUser:
      type: object
      required: [user_id, username, display_name, followed_at]
      description: User profile information for follow relationships
      properties:
        user_id:
          type: string
          format: uuid
          description: Unique identifier of the user
          example: 123e4567-e89b-12d3-a456-426614174000
        username:
          type: string
          description: Unique username
          minLength: 3
          maxLength: 50
          example: johndoe
        display_name:
          type: string
          description: User's display name
          maxLength: 100
          example: John Doe
        bio:
          type: string
          description: User's biography
          maxLength: 500
          nullable: true
          example: Photographer and nature enthusiast
        followed_at:
          type: string
          format: date-time
          description: When the follow relationship was created
          example: 2024-01-15T10:30:00Z

    FollowersList:
      type: object
      required: [followers, total_count, offset, limit]
      description: Paginated list of followers
      properties:
        followers:
          type: array
          description: List of users who are following the specified user
          items:
            $ref: '#/components/schemas/FollowUser'
        total_count:
          type: integer
          description: Total number of followers
          minimum: 0
          example: 150
        offset:
          type: integer
          description: Number of items skipped for pagination
          minimum: 0
          example: 0
        limit:
          type: integer
          description: Maximum number of items returned
          minimum: 1
          maximum: 100
          example: 20
      example:
        followers:
          - user_id: 123e4567-e89b-12d3-a456-426614174001
            username: follower1
            display_name: Follower One
            bio: Photography enthusiast
            followed_at: 2024-01-15T10:30:00Z
          - user_id: 123e4567-e89b-12d3-a456-426614174002
            username: follower2
            display_name: Follower Two
            bio: Nature lover
            followed_at: 2024-01-14T09:15:00Z
        total_count: 150
        offset: 0
        limit: 20

    FollowingList:
      type: object
      required: [following, total_count, offset, limit]
      description: Paginated list of users being followed
      properties:
        following:
          type: array
          description: List of users that the specified user is following
          items:
            $ref: '#/components/schemas/FollowUser'
        total_count:
          type: integer
          description: Total number of users being followed
          minimum: 0
          example: 75
        offset:
          type: integer
          description: Number of items skipped for pagination
          minimum: 0
          example: 0
        limit:
          type: integer
          description: Maximum number of items returned
          minimum: 1
          maximum: 100
          example: 20
      example:
        following:
          - user_id: 123e4567-e89b-12d3-a456-426614174003
            username: followed1
            display_name: Followed User One
            bio: Artist and creator
            followed_at: 2024-01-13T14:20:00Z
          - user_id: 123e4567-e89b-12d3-a456-426614174004
            username: followed2
            display_name: Followed User Two
            bio: Travel photographer
            followed_at: 2024-01-12T11:45:00Z
        total_count: 75
        offset: 0
        limit: 20

    ActivityFeed:
      type: object
      required: [activities, total_count, offset, limit]
      description: Paginated feed of activities from followed users
      properties:
        activities:
          type: array
          description: List of activities from followed users
          items:
            $ref: '#/components/schemas/Activity'
        total_count:
          type: integer
          description: Total number of activities in the feed
          minimum: 0
          example: 150
        offset:
          type: integer
          description: Number of activities skipped for pagination
          minimum: 0
          example: 0
        limit:
          type: integer
          description: Maximum number of activities returned
          minimum: 1
          maximum: 100
          example: 20
      example:
        activities:
          - id: 123e4567-e89b-12d3-a456-426614174010
            actor_id: 123e4567-e89b-12d3-a456-426614174003
            activity_type: image_uploaded
            target_type: image
            target_id: 123e4567-e89b-12d3-a456-426614174011
            metadata:
              image_title: Beautiful sunset
              image_url: https://cdn.goimg.com/images/123e4567/medium.jpg
            created_at: 2024-01-15T14:20:00Z
          - id: 123e4567-e89b-12d3-a456-426614174012
            actor_id: 123e4567-e89b-12d3-a456-426614174004
            activity_type: user_followed
            target_type: user
            target_id: 123e4567-e89b-12d3-a456-426614174005
            metadata:
              followed_username: photographer123
            created_at: 2024-01-15T13:45:00Z
        total_count: 150
        offset: 0
        limit: 20

    Activity:
      type: object
      required: [id, actor_id, activity_type, target_type, target_id, created_at]
      description: A single activity performed by a user
      properties:
        id:
          type: string
          format: uuid
          description: Unique identifier for the activity
          example: 123e4567-e89b-12d3-a456-426614174010
        actor_id:
          type: string
          format: uuid
          description: ID of the user who performed the activity
          example: 123e4567-e89b-12d3-a456-426614174003
        activity_type:
          type: string
          enum: [image_uploaded, image_liked, image_commented, user_followed, album_created]
          description: Type of activity performed
          example: image_uploaded
        target_type:
          type: string
          enum: [image, user, album, comment]
          description: Type of the target entity
          example: image
        target_id:
          type: string
          format: uuid
          description: ID of the target entity
          example: 123e4567-e89b-12d3-a456-426614174011
        metadata:
          type: object
          additionalProperties:
            type: string
          description: Additional contextual information about the activity
          example:
            image_title: Beautiful sunset
            image_url: https://cdn.goimg.com/images/123e4567/medium.jpg
        created_at:
          type: string
          format: date-time
          description: When the activity occurred
          example: 2024-01-15T14:20:00Z

    # ==================== Authentication ====================
    TokenResponse:
      type: object
      required: [access_token, refresh_token, token_type, expires_in]
      properties:
        access_token:
          type: string
          example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
        refresh_token:
          type: string
          example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
        token_type:
          type: string
          example: Bearer
        expires_in:
          type: integer
          description: Access token lifetime in seconds
          example: 900

    # ==================== Two-Factor Authentication ====================
    Setup2FAResponse:
      type: object
      required: [secret, provisioning_uri, issuer, account_name, backup_codes]
      description: Response from 2FA setup containing secret and backup codes
      properties:
        secret:
          type: string
          description: Base32-encoded TOTP secret for manual entry into authenticator
          example: JBSWY3DPEHPK3PXP
        provisioning_uri:
          type: string
          format: uri
          description: otpauth:// URI for QR code generation
          example: otpauth://totp/goimg:user@example.com?secret=JBSWY3DPEHPK3PXP&issuer=goimg
        issuer:
          type: string
          description: Application name shown in authenticator apps
          example: goimg
        account_name:
          type: string
          description: Account identifier (usually email)
          example: user@example.com
        backup_codes:
          type: array
          description: One-time recovery codes (10 codes, shown only once)
          items:
            type: string
          minItems: 10
          maxItems: 10
          example:
            - "A1B2C3D4"
            - "E5F6G7H8"
            - "I9J0K1L2"
            - "M3N4O5P6"
            - "Q7R8S9T0"
            - "U1V2W3X4"
            - "Y5Z6A7B8"
            - "C9D0E1F2"
            - "G3H4I5J6"
            - "K7L8M9N0"

    TwoFactorStatus:
      type: object
      required: [enabled, setup_pending, backup_codes_remaining]
      description: Current 2FA status for a user
      properties:
        enabled:
          type: boolean
          description: Whether 2FA is currently active
          example: true
        setup_pending:
          type: boolean
          description: Whether 2FA setup was started but not yet verified
          example: false
        backup_codes_remaining:
          type: integer
          description: Number of unused backup codes remaining
          minimum: 0
          maximum: 10
          example: 8
        enabled_at:
          type: string
          format: date-time
          description: When 2FA was enabled (null if not enabled)
          nullable: true
          example: 2024-01-15T10:30:00Z

    BackupCodesResponse:
      type: object
      required: [backup_codes, remaining_codes]
      description: Response containing newly generated backup codes
      properties:
        backup_codes:
          type: array
          description: New one-time recovery codes (shown only once)
          items:
            type: string
          minItems: 10
          maxItems: 10
          example:
            - "A1B2C3D4"
            - "E5F6G7H8"
            - "I9J0K1L2"
            - "M3N4O5P6"
            - "Q7R8S9T0"
            - "U1V2W3X4"
            - "Y5Z6A7B8"
            - "C9D0E1F2"
            - "G3H4I5J6"
            - "K7L8M9N0"
        remaining_codes:
          type: integer
          description: Number of unused backup codes (always 10 after regeneration)
          example: 10

    # ==================== OAuth 2.0 ====================
    OAuthAccount:
      type: object
      required: [provider, provider_user_id, email, linked_at]
      description: OAuth account linked to a user
      properties:
        provider:
          type: string
          enum: [google, github]
          description: OAuth provider name
          example: google
        provider_user_id:
          type: string
          description: Unique user ID from the OAuth provider
          example: "103547991597142817347"
        email:
          type: string
          format: email
          description: Email address from the OAuth provider
          example: user@example.com
        display_name:
          type: string
          description: Display name from the OAuth provider
          nullable: true
          example: John Doe
        avatar_url:
          type: string
          format: uri
          description: Profile picture URL from the OAuth provider
          nullable: true
          example: https://lh3.googleusercontent.com/a/...
        linked_at:
          type: string
          format: date-time
          description: When the OAuth account was linked to the user
          example: 2024-01-15T10:30:00Z

    # ==================== Pagination ====================
    PaginatedResponse:
      type: object
      required: [items, pagination]
      properties:
        items:
          type: array
          items: {}
        pagination:
          $ref: '#/components/schemas/Pagination'

    Pagination:
      type: object
      required: [total, page, per_page, total_pages]
      properties:
        total:
          type: integer
          description: Total number of items
          example: 150
        page:
          type: integer
          description: Current page number
          example: 1
        per_page:
          type: integer
          description: Items per page
          example: 20
        total_pages:
          type: integer
          description: Total number of pages
          example: 8

    # ==================== Error Responses (RFC 7807) ====================
    ProblemDetail:
      type: object
      required: [type, title, status]
      properties:
        type:
          type: string
          format: uri
          description: A URI reference that identifies the problem type
          example: https://api.goimg.com/problems/validation-error
        title:
          type: string
          description: A short, human-readable summary of the problem type
          example: Validation Error
        status:
          type: integer
          description: The HTTP status code
          example: 400
        detail:
          type: string
          description: A human-readable explanation specific to this occurrence
          example: Request validation failed
        instance:
          type: string
          format: uri
          description: A URI reference that identifies the specific occurrence
          example: /api/v1/images
        traceId:
          type: string
          description: A unique identifier for tracing the request
          example: abc123-def456
        errors:
          type: array
          description: Detailed validation errors (for 400 responses)
          items:
            type: object
            properties:
              field:
                type: string
                example: title
              message:
                type: string
                example: must not exceed 255 characters

    # ==================== Session ====================
    Session:
      type: object
      required: [id, user_id, created_at, expires_at]
      description: User session information
      properties:
        id:
          type: string
          format: uuid
          description: Session ID
          example: 123e4567-e89b-12d3-a456-426614174000
        user_id:
          type: string
          format: uuid
          description: User ID
          example: 123e4567-e89b-12d3-a456-426614174001
        ip_address:
          type: string
          description: IP address of the session
          example: 192.168.1.1
        user_agent:
          type: string
          description: Browser/device user agent
          example: Mozilla/5.0 (Windows NT 10.0; Win64; x64)
        created_at:
          type: string
          format: date-time
          description: Session creation time
          example: 2024-01-15T10:30:00Z
        expires_at:
          type: string
          format: date-time
          description: Session expiration time
          example: 2024-01-22T10:30:00Z
        is_current:
          type: boolean
          description: Whether this is the current session
          example: true

    # ==================== Notifications ====================
    NotificationsList:
      type: object
      required: [notifications, totalUnread, offset, limit]
      description: Paginated list of notifications with unread count
      properties:
        notifications:
          type: array
          description: List of notifications for the user
          items:
            $ref: '#/components/schemas/Notification'
        totalUnread:
          type: integer
          description: Total number of unread notifications
          minimum: 0
          example: 5
        offset:
          type: integer
          description: Number of notifications skipped for pagination
          minimum: 0
          example: 0
        limit:
          type: integer
          description: Maximum number of notifications returned
          minimum: 1
          maximum: 100
          example: 20
      example:
        notifications:
          - id: 123e4567-e89b-12d3-a456-426614174001
            type: new_follower
            title: johndoe started following you
            body: johndoe is now following you. View their profile to see their photos.
            metadata:
              follower_id: 123e4567-e89b-12d3-a456-426614174002
              follower_username: johndoe
            isRead: false
            createdAt: 2024-01-15T14:20:00Z
            readAt: null
          - id: 123e4567-e89b-12d3-a456-426614174003
            type: new_photos
            title: 5 new photos from users you follow
            body: Check out the latest photos from your network.
            metadata:
              photo_count: "5"
            isRead: true
            createdAt: 2024-01-15T10:15:00Z
            readAt: 2024-01-15T11:30:00Z
        totalUnread: 5
        offset: 0
        limit: 20

    Notification:
      type: object
      required: [id, type, title, body, metadata, isRead, createdAt]
      description: A single notification for a user
      properties:
        id:
          type: string
          format: uuid
          description: Unique identifier for the notification
          example: 123e4567-e89b-12d3-a456-426614174001
        type:
          type: string
          enum: [new_follower, new_photos, account_suspended, account_banned, account_reinstated, abuse_report, report_escalated, mod_action_required]
          description: Type of notification
          example: new_follower
        title:
          type: string
          maxLength: 255
          description: Notification title (short summary)
          example: johndoe started following you
        body:
          type: string
          description: Notification body (detailed message)
          example: johndoe is now following you. View their profile to see their photos.
        metadata:
          type: object
          description: Flexible JSON payload for notification-specific data
          additionalProperties:
            type: string
          example:
            follower_id: 123e4567-e89b-12d3-a456-426614174002
            follower_username: johndoe
        isRead:
          type: boolean
          description: Whether the notification has been read
          example: false
        createdAt:
          type: string
          format: date-time
          description: When the notification was created
          example: 2024-01-15T14:20:00Z
        readAt:
          type: string
          format: date-time
          description: When the notification was marked as read (null if unread)
          nullable: true
          example: null

    UnreadCount:
      type: object
      required: [count]
      description: Count of unread notifications for badge display
      properties:
        count:
          type: integer
          description: Number of unread notifications
          minimum: 0
          example: 5
      example:
        count: 5

    # ==================== Health ====================
    HealthStatus:
      type: object
      required: [status, timestamp]
      properties:
        status:
          type: string
          enum: [ok, degraded, down]
          example: ok
        timestamp:
          type: string
          format: date-time
          example: 2024-01-15T10:30:00Z

    HealthReadyResponse:
      type: object
      required: [status, timestamp, checks]
      description: Readiness check response with dependency health status
      properties:
        status:
          type: string
          enum: [ready, not_ready]
          example: ready
        timestamp:
          type: string
          format: date-time
          example: 2024-01-15T10:30:00Z
        checks:
          type: object
          properties:
            database:
              $ref: '#/components/schemas/HealthCheck'
            redis:
              $ref: '#/components/schemas/HealthCheck'

    HealthCheck:
      type: object
      description: Individual dependency health check result
      properties:
        status:
          type: string
          enum: [up, down]
          example: up
        latency_ms:
          type: number
          format: float
          description: Response time in milliseconds
          example: 5.2
        error:
          type: string
          description: Error message if check failed
          example: connection refused
