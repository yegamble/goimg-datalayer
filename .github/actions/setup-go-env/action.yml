name: 'Setup Go Environment'
description: 'Setup Go 1.25+ and install dependencies like libvips'
inputs:
  go-version:
    description: 'Go version to install'
    required: false
    default: '1.25.x'
  cache:
    description: 'Enable Go module caching'
    required: false
    default: 'true'
  install-libvips:
    description: 'Install libvips dependency'
    required: false
    default: 'true'

runs:
  using: 'composite'
  steps:
    - name: Set up Go
      uses: actions/setup-go@0a12ed9d6a96ab950c8f026ed9f722fe0da7ef32 # v5.0.2
      with:
        go-version: ${{ inputs.go-version }}
        cache: ${{ inputs.cache }}

    - name: Install libvips (Linux)
      if: inputs.install-libvips == 'true' && runner.os == 'Linux'
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -y libvips-dev

    - name: Install libvips (macOS)
      if: inputs.install-libvips == 'true' && runner.os == 'macOS'
      shell: bash
      run: |
        brew install vips
